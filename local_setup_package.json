{
  "name": "employee-directory-local-setup",
  "version": "1.0.0",
  "description": "Complete Employee Directory application with Google Sheets integration",
  "scripts": {
    "setup-backend": "cd backend && python -m venv venv && pip install -r requirements.txt",
    "setup-frontend": "cd frontend && npm install",
    "start-backend": "cd backend && python server.py",
    "start-frontend": "cd frontend && npm start",
    "dev": "concurrently \"npm run start-backend\" \"npm run start-frontend\""
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  },
  "keywords": ["employee-directory", "react", "fastapi", "mongodb", "google-sheets"],
  "author": "Employee Directory System",
  "license": "MIT"
}